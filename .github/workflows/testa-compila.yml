name: Testa Compila

on: workflow_dispatch

jobs:
    build-commit:
        runs-on: ubuntu-latest
        steps:
            - run: echo "Nova build"
    
    test-commit:
        runs-on: ubuntu-latest
        steps:
            - run: echo "Testa nova build"